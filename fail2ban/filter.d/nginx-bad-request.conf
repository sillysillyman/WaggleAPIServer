[Definition]
# 일반적인 정찰/공격 패턴만 ban — 정상 사용자의 404/403 트리거 방지
failregex = ^<HOST> -.* "(GET|POST|HEAD) /(wp-admin|wp-login|wp-content|wordpress|\.env|\.git|\.aws|\.ssh|phpmyadmin|pma|adminer|admin\.php|xmlrpc\.php|cgi-bin|setup\.php|owa) .* HTTP/[\d.]+" \d+ .*$
            ^<HOST> -.* "[A-Z]+ /[^"]*\.(php|asp|aspx|jsp|cgi|env|sql|bak|backup|old) .* HTTP/[\d.]+" \d+ .*$
            ^<HOST> -.* "[A-Z]+ .+" 444 .*$
ignoreregex =
